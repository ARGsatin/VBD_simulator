# VBD_simulator 项目结构说明

## 项目概述

本项目是**水凝胶 DLP（数字光处理）打印 VBD（顶点块阻尼）仿真系统**，用于模拟水凝胶光固化逐层打印过程中的物理形变，并通过电场补偿控制来减小悬垂结构等打印误差。

**核心技术路线**：Python 做系统控制与数据处理，纯 Python 实现 VBD 求解器原型（可验证逻辑的简化版），未来可将求解核心迁移到 C++。

---

## 目录结构

```
VBD_simulator/
│
├── README.md                    # 项目说明（GitHub 首页）
├── 项目结构说明.md                # ← 你正在看的文档
├── pyproject.toml               # Python 项目配置、依赖、构建系统
├── run_gui.py                   # GUI 启动入口（双击或 python run_gui.py）
├── .gitignore                   # Git 忽略规则
│
├── src/                         # 🐍 核心源码（Python 包）
│   └── hydrogel_vbd/            # 主包
│       ├── __init__.py          # 包初始化，暴露公共 API
│       ├── config.py            # 配置加载与管理
│       ├── state.py             # 仿真状态管理（网格、速度、激活掩码）
│       ├── main_loop.py         # 主仿真循环（逐层激活→求解→电场补偿）
│       │
│       ├── solver/              # 🔧 VBD 求解器
│       │   ├── vbd_solver.py        # 显式/隐式 VBD 求解步骤
│       │   ├── elastic_energy.py    # 四面体弹性能量计算
│       │   ├── constraints.py       # 约束条件（Dirichlet 边界等）
│       │   └── graph_coloring.py    # 图着色算法（并行 VBD 更新顺序）
│       │
│       ├── forces/              # 🌊 多物理场力模型
│       │   ├── aggregate.py         # 合力聚合器
│       │   ├── gravity.py           # 重力
│       │   ├── surface_tension.py   # 表面张力
│       │   ├── fluid_drag.py        # 流体拖曳力
│       │   ├── peel.py              # 剥离力（离型膜作用力）
│       │   ├── czm.py               # 内聚区模型（CZM，界面脱粘）
│       │   ├── electric.py          # 电场力
│       │   └── local_terms.py       # 局部物理项（含流体截止）
│       │
│       ├── material/            # 🧪 水凝胶材料模型
│       │   └── hydrogel_model.py    # 基于固化度的弹性模量演化
│       │
│       ├── geometry/            # 📐 几何处理管线
│       │   ├── stl_mesher.py        # STL 读取 → 四面体网格生成
│       │   ├── layer_activator.py   # 逐层激活逻辑
│       │   └── conformal_pipeline.py# Conformal 网格管线
│       │
│       ├── control/             # 🎛️ 控制与优化
│       │   ├── field_controller.py  # 电场 PID 控制器
│       │   └── voltage_optimizer.py # 电压优化器
│       │
│       ├── evaluation/          # 📊 评估与误差
│       │   ├── metrics.py           # 评估指标计算
│       │   └── shape_error.py       # 形状误差分析
│       │
│       ├── io/                  # 💾 输入输出
│       │   ├── npz_state.py         # NPZ 状态文件读写
│       │   ├── vtk_writer.py        # VTK/VTU 可视化文件导出
│       │   ├── report_writer.py     # CSV/JSON 报告生成
│       │   └── gcode_exporter.py    # G-code 导出（含电场补偿指令）
│       │
│       └── gui/                 # 🖥️ 图形用户界面
│           ├── main_window.py       # PySide6 主窗口
│           └── __init__.py          # GUI 启动函数
│
├── tests/                       # ✅ 单元测试
│   ├── test_state_and_activation.py    # 状态管理与层激活测试
│   ├── test_models_solver_control.py   # 力模型、求解器、控制测试
│   ├── test_multiphysics_vbd_pid.py    # 多物理场 + PID 测试
│   ├── test_lift_and_gui.py            # 提升求解 + GUI 测试
│   ├── test_io_and_main_loop.py        # IO + 主循环测试
│   ├── test_conformal_architecture.py  # Conformal 网格架构测试
│   └── test_package_and_configs.py     # 包导入 + 配置模板测试
│
├── configs/                     # ⚙️ 配置文件
│   ├── config.yaml                  # 主配置模板
│   ├── material_hydrogel.json       # 水凝胶材料参数
│   ├── printer_dlp.json             # DLP 打印机参数
│   ├── solver_vbd.json              # VBD 求解器参数
│   └── electrode_config.json        # 电极/电场配置
│
├── assets/                      # 🗂️ 静态资源
│   └── test_models/                 # 测试用 3D 模型
│       ├── demo7(1).STL              # Demo 7 模型
│       └── 长方体(1).STL             # 长方体测试模型
│
├── docs/                        # 📚 文档
│   ├── roadmap.md                  # 项目开发路线图（原 hydrogel_dlp_vbd_simulation_roadmap.md）
│   ├── VBD论文.pdf                 # 相关论文参考
│   │
│   ├── architecture/              # 架构设计文档
│   │   ├── 架构.docx               # 系统架构说明（原根目录 架构.docx）
│   │   ├── 技术栈.md               # 技术栈选型分析（原根目录 技术栈.md）
│   │   └── 伪代码.md               # 算法伪代码（原根目录 伪代码）
│   │
│   ├── guide/                     # 开发指南
│   │   ├── 修改点.docx            # 修改记录（原根目录 修改点.docx）
│   │   └── 各种力.pdf             # 物理力模型参考资料（原根目录 各种力.pdf）
│   │
│   └── superpowers/               # Superpowers 集成文档
│       └── plans/
│           └── 2026-05-04-hydrogel-vbd-sim-framework.md
│
└── outputs/                     # 📤 仿真输出（运行时生成，已加入 .gitignore）
    └── <run_name>/               # 每次运行的输出目录
        ├── states/                # 逐层 NPZ 状态文件
        ├── vtk/                   # ParaView VTU 可视化文件
        ├── gcode/                 # 补偿后 G-code 文件
        ├── reports/               # 误差指标 CSV 报告
        └── simulation_field_commands.json  # 电场指令记录
```

---

## 快速开始

### 1. 安装依赖
```bash
cd VBD_simulator
pip install -e .
```

### 2. 运行仿真
```bash
# 命令行运行 2 层仿真
python src/hydrogel_vbd/main_loop.py --layers 2

# 启动图形界面
python run_gui.py
```

### 3. 运行测试
```bash
python -m pytest tests/ -v
```
所有测试均通过（29/29 ✅）。

---

## 关键文件说明

| 文件 | 用途 | 阅读顺序 |
|------|------|----------|
| `src/hydrogel_vbd/main_loop.py` | 仿真主循环，理解整体流程的入口 | 1️⃣ |
| `src/hydrogel_vbd/state.py` | 网格状态定义（顶点、四面体、激活掩码） | 2️⃣ |
| `src/hydrogel_vbd/solver/vbd_solver.py` | VBD 求解核心 | 3️⃣ |
| `src/hydrogel_vbd/forces/aggregate.py` | 合力聚合逻辑 | 4️⃣ |
| `src/hydrogel_vbd/control/field_controller.py` | PID 电场补偿控制器 | 5️⃣ |
| `src/hydrogel_vbd/geometry/layer_activator.py` | 逐层激活策略 | 6️⃣ |
| `src/hydrogel_vbd/io/gcode_exporter.py` | 补偿后 G-code 生成 | 7️⃣ |
| `configs/config.yaml` | 所有可调参数的入口 | 配置 |
| `tests/` | 各模块的测试范例 | 参考 |

---

## 论文与参考资料

- `docs/VBD论文.pdf` — VBD（Vertex Block Descent）方法相关论文
- `docs/guide/各种力.pdf` — 多物理场力模型（重力、表面张力、流体拖曳、剥离力、CZM）的参考资料

---

## 维护说明

- `.gitignore` 已配置忽略 `outputs/`、`__pycache__/`、`.pytest_cache/` 等生成文件
- 测试模型存放在 `assets/test_models/`，测试代码通过相对路径引用
- 配置文件位于 `configs/`，主配置为 `config.yaml`，子配置可独立覆盖
