# VBD_simulator 项目结构说明

## 项目概述

本项目是**水凝胶 DLP（数字光处理）打印 VBD（顶点块阻尼）仿真系统**，用于模拟水凝胶光固化逐层打印过程中的物理形变，并通过电场补偿控制来减小悬垂结构等打印误差。

**核心技术路线**：
- **Python 层**（`src/hydrogel_vbd/`）：系统控制、数据处理、参数配置、VBD 求解器纯 Python 参考实现
- **C++ 加速层**（`cpp/`）：pybind11 绑定，将 VBD 核心求解器、弹性能量计算和物理项迁移到 C++ 以提升性能
- **GUI**（PySide6）：四步工作流（加载模型 → 划分网格 → 运行仿真 → 结果汇总），集成 3D 网格预览、动画回放、电场-误差分析图表

---

## 目录结构

```
VBD_simulator/
│
├── README.md                    # 项目说明（GitHub 首页）
├── 项目结构说明.md                # ← 你正在看的文档
├── pyproject.toml               # Python 项目配置、依赖、构建系统
├── run_gui.py                   # GUI 启动入口（python run_gui.py）
├── .gitignore                   # Git 忽略规则（outputs/、__pycache__/ 等）
│
├── cpp/                         # ⚡ C++ 加速层（pybind11 绑定）
│   ├── CMakeLists.txt               # CMake 构建配置
│   ├── bindings/
│   │   └── pybind_vbd.cpp           # pybind11 绑定入口（暴露 VBD 函数到 Python）
│   ├── include/
│   │   ├── types.h                  # C++ 类型定义（Vec3、Mat3、四面体等）
│   │   ├── vbd_solver.h             # VBD 求解器核心接口
│   │   ├── elastic_energy.h         # Neo-Hookean 弹性能量与 Hessian
│   │   └── physics_terms.h          # 物理力项（重力、电场、拖曳、CZM 等）
│   └── src/
│       ├── vbd_solver.cpp           # VBD 求解器实现
│       ├── elastic_energy.cpp       # 弹性能量计算
│       └── physics_terms.cpp        # 物理力项实现
│
├── src/                         # 🐍 核心源码（Python 包）
│   └── hydrogel_vbd/            # 主包
│       ├── __init__.py          # 包初始化，暴露公共 API
│       │
│       ├── core/                # 🧠 核心层：配置、状态、主循环
│       │   ├── config.py            # SimulationConfig：16 个调优参数的配置模型
│       │   ├── state.py             # 仿真状态管理（网格顶点/四面体、速度、激活掩码）
│       │   └── main_loop.py         # 主仿真循环（逐层激活→求解→电场补偿）
│       │
│       ├── solver/              # 🔧 VBD 求解器
│       │   ├── vbd_solver.py        # Python 参考 VBD 求解器（含提升动力学 solve_with_lift）
│       │   ├── constraints.py       # Dirichlet 边界约束
│       │   └── graph_coloring.py    # 图着色算法（并行 VBD 更新顺序）
│       │
│       ├── physics/             # 🌊⚡🧪 物理层：力模型 + 本构 + 弹性能量
│       │   ├── aggregate.py         # 合力聚合器（统一力场管线）
│       │   ├── gravity.py           # 重力
│       │   ├── surface_tension.py   # 表面张力
│       │   ├── fluid_drag.py        # 流体拖曳力
│       │   ├── peel.py              # 剥离力（离型膜作用力）
│       │   ├── czm.py               # 内聚区模型（CZM，界面脱粘与失效）
│       │   ├── electric.py          # 电场力
│       │   ├── local_terms.py       # 局部物理项（流体截止、稳定化阻尼）
│       │   ├── elastic_energy.py    # 四面体 Neo-Hookean 弹性能量
│       │   └── hydrogel_model.py    # 水凝胶材料模型（固化度→Lamé 常数）
│       │
│       ├── geometry/            # 📐 几何处理管线
│       │   ├── stl_mesher.py        # CAD → 四面体网格（支持 STL/STEP、OCC Boolean Fragment）
│       │   ├── layer_activator.py   # 逐层激活逻辑（继承激活策略）
│       │   └── conformal_pipeline.py# Conformal 网格管线（保形四面体剖分）
│       │
│       ├── control/             # 🎛️ 控制与优化
│       │   ├── field_controller.py  # 电场 PID 控制器（K_p/K_i/K_d 可调）
│       │   └── voltage_optimizer.py # 电压优化器（电场强度→电极电压映射）
│       │
│       ├── evaluation/          # 📊 评估与误差
│       │   ├── metrics.py           # 评估指标计算
│       │   └── shape_error.py       # 形状误差分析
│       │
│       ├── io/                  # 💾 输入输出
│       │   ├── npz_state.py         # NPZ 状态文件读写（逐层快照）
│       │   ├── vtk_writer.py        # VTK/VTU 可视化文件导出（ParaView）
│       │   ├── report_writer.py     # CSV 误差指标报告生成
│       │   └── gcode_exporter.py    # G-code 导出（含电场补偿指令）
│       │
│       └── gui/                 # 🖥️ 图形用户界面（PySide6）
│           ├── main_window.py       # 主窗口：四步工作流 + 动画回放 + 电场-误差分析
│           ├── mesh_viewer.py       # 3D 网格渲染器（初始 / 变形 / 动画帧）
│           ├── simulation_worker.py # QThread 物理工作线程
│           └── __init__.py          # GUI 启动入口（launch_gui）
│
├── tests/                       # ✅ 单元测试（共 7 个）
│   ├── test_state_and_activation.py    # 状态管理与层激活测试
│   ├── test_models_solver_control.py   # 力模型、求解器、控制测试
│   ├── test_multiphysics_vbd_pid.py    # 多物理场 + PID 控制测试
│   ├── test_lift_and_gui.py            # 提升动力学求解 + GUI 测试
│   ├── test_io_and_main_loop.py        # IO + 主循环集成测试
│   ├── test_conformal_architecture.py  # Conformal 网格架构测试
│   └── test_package_and_configs.py     # 包导入 + 配置模板测试
│
├── configs/                     # ⚙️ 配置文件（YAML/JSON）
│   ├── config.yaml                  # 主配置模板（全部参数入口）
│   ├── material_hydrogel.json       # 水凝胶材料参数（固化特性）
│   ├── printer_dlp.json             # DLP 打印机机械参数
│   ├── solver_vbd.json              # VBD 求解器数值参数
│   └── electrode_config.json        # 电极/电场配置
│
├── assets/                      # 🗂️ 静态资源
│   └── test_models/                 # 测试用 3D 模型
│       ├── 长方体(1).STL             # 长方体测试模型（STL 格式）
│       ├── demo7(1).STL              # Demo 7 模型（STL 离散三角面片）
│       └── demo7.STEP               # Demo 7 模型（STEP B-Rep 精确几何）
│
├── docs/                        # 📚 项目文档
│   ├── roadmap.md                  # 项目开发路线图
│   ├── architecture/               # 架构设计文档
│   │   ├── 架构.docx               # 系统架构说明
│   │   ├── 技术栈.md               # 技术栈选型分析
│   │   └── 伪代码.md               # 核心算法伪代码
│   ├── guide/                      # 开发指南
│   │   ├── 修改点.docx             # 修改记录
│   │   └── (各种力.pdf)            # 多物理场力模型参考
│   └── superpowers/                # 开发计划文档
│       └── plans/
│           └── 2026-05-04-hydrogel-vbd-sim-framework.md
│
└── outputs/                     # 📤 仿真输出（运行时自动生成，已加入 .gitignore）
    └── <run_name>/               # 每次运行的独立输出目录
        ├── states/                # 逐层 NPZ 状态文件
        ├── vtk/                   # ParaView VTU 可视化文件
        ├── gcode/                 # 补偿后 G-code 文件
        └── reports/               # 误差指标 CSV 报告
```

---

## 快速开始

### 1. 安装依赖
```bash
cd VBD_simulator
pip install -e .
```

### 2. 运行仿真

```bash
# 命令行批量运行 2 层仿真
python src/hydrogel_vbd/core/main_loop.py --layers 2

# 启动图形界面（四步工作流）
python run_gui.py
```

### 3. 运行测试
```bash
python -m pytest tests/ -v
```

---

## GUI 架构概览

`MainWindow`（`src/hydrogel_vbd/gui/main_window.py`）提供完整交互式仿真体验：

### 四步工作流
1. **加载模型**：支持 STL / STEP 格式，自动计算包围盒、层厚、XY 分辨率
2. **划分网格**：Gmsh OCC Boolean Fragment 生成分层共形四面体网格，**支持两种算法**：
   - 规整分层算法（OCC 切片，保证四面体不跨层）
   - 标准非结构化算法（自由四面体）
3. **运行仿真**：逐层激活 → CZM 剥离力施加 → VBD 求解 → PID 电场补偿；求解过程**实时渲染降频优化**（50 步间隔 + 0.1 mm 形变阈值强制刷新），避免 GUI 无响应
4. **结果汇总**：CSV 误差报告 + 逐层 VTK 可视化 + 电场-误差分析图窗

### 左侧参数面板
- `ParameterPanel`（`QScrollArea` 包裹，**防止窗口高度不足时控件被挤压**），16 个水凝胶准静态拉拔参数：
  - 材料参数：μ（剪切模量）、κ（体积模量）、k_d（阻尼系数）、c_shrink（收缩因子）
  - 附着参数：T_max（最大附着力）、K_czm（CZM 刚度）、δ_f（失效位移）
  - 数值参数：dt（时间步长）、max_iters（最大迭代次数）、N_stable（稳定步数判决）
  - 工艺参数：层厚、提升速度 v_lift
  - 控制参数：PID K_p / K_i / K_d、最大电场 E_max
- 控制信息栏元素间使用 `addSpacing(15)` 隔开，保持视觉呼吸感

### 后处理动画与可视化
- **3D 网格渲染**：`MeshViewer` 组件显示初始四面体网格和实时变形结果
- **动画回放**：仿真过程中缓存所有物理帧，仿真完成后**自动启动 ~30 FPS 循环回放**
  - 支持 播放 / 暂停 / 停止 控制
  - 支持按层筛选动画帧
  - 水平帧滑块可拖动定位到任意帧
- **电场-误差分析窗口**：仿真结束时自动弹出，嵌入 matplotlib 双面板图表：
  - 左上：E_z 电场强度逐层变化折线图（右侧 y 轴为累计误差百分比填充）
  - 右下：max_error 与 rms_error 双线对比图
  - 支持 PNG / SVG 保存

---

## C++ 加速层结构

| 文件 | 用途 |
|------|------|
| `cpp/include/types.h` | C++ 核心类型：`Vec3`、`Mat3`、四面体索引等轻量数据结构 |
| `cpp/include/vbd_solver.h` | VBD 求解器接口：`solve_step()`、`solve_until_stable()` |
| `cpp/include/elastic_energy.h` | Neo-Hookean 弹性能量 → `energy()`、`gradient()`、`hessian()` |
| `cpp/include/physics_terms.h` | 物理力项：重力、电场力、流体拖曳、CZM 等 |
| `cpp/bindings/pybind_vbd.cpp` | pybind11 绑定：将上述 C++ 函数暴露为 Python 可调用对象 |

构建方式（需安装 `pybind11` 和 `Eigen`）：
```bash
cd cpp && mkdir build && cd build
cmake .. && make -j$(nproc)
```

---

## 关键文件阅读顺序

| # | 文件 | 用途 | 层级 |
|---|------|------|------|
| 1 | `src/hydrogel_vbd/core/config.py` | 了解全部 16 个可调参数的定义、默认值与取值范围 | 配置 |
| 2 | `src/hydrogel_vbd/core/state.py` | 网格状态数据结构：顶点、速度、四面体、激活掩码 | 数据 |
| 3 | `src/hydrogel_vbd/physics/hydrogel_model.py` | 水凝胶本构模型：固化度 → Lamé 常数映射 | 物理 |
| 4 | `src/hydrogel_vbd/physics/elastic_energy.py` | Neo-Hookean 四面体弹性能量、梯度与 Hessian | 物理 |
| 5 | `src/hydrogel_vbd/physics/czm.py` | 内聚区模型：CZM 附着 / 脱粘状态机与判据 | 物理 |
| 6 | `src/hydrogel_vbd/solver/vbd_solver.py` | VBD 求解核心：Chebyshev 半隐式 + 提升动力学 | 求解 |
| 7 | `src/hydrogel_vbd/control/field_controller.py` | PID 电场控制器：`K_p` / `K_i` / `K_d` 反馈迭代 | 控制 |
| 8 | `src/hydrogel_vbd/geometry/stl_mesher.py` | CAD → 四面体网格管线（OCC B-Rep + Boolean Fragment） | 几何 |
| 9 | `src/hydrogel_vbd/gui/main_window.py` | PySide6 主窗口：四步工作流 + 动画回放 + 分析图窗 | GUI |
| 10 | `cpp/bindings/pybind_vbd.cpp` | C++ 加速绑定入口（了解 Python ↔ C++ 接口） | 加速 |
| 11 | `configs/config.yaml` | 所有可调参数的总入口 | 配置 |

---

## 测试策略

| 测试文件 | 覆盖范围 |
|----------|----------|
| `test_state_and_activation.py` | MeshState 生命周期、层激活继承策略 |
| `test_models_solver_control.py` | 力模型正确性、VBD 求解收敛性、PID 反馈 |
| `test_multiphysics_vbd_pid.py` | 多物理场耦合 + PID 控制闭环集成 |
| `test_lift_and_gui.py` | 提升动力学 (solve_with_lift) + GUI 组件初始化 |
| `test_io_and_main_loop.py` | NPZ/VTK/CSV 读写 + 主仿真循环端到端 |
| `test_conformal_architecture.py` | Conformal 网格管线与分层质量 |
| `test_package_and_configs.py` | 包导入完整性 + 配置文件模板校验 |

运行全部测试：
```bash
python -m pytest tests/ -v
```

---

## 近期架构修复清单（2026-05-11 全盘审查）

以下为针对 3 个致命漏洞（Fatal）和 1 个架构隐患（Warning）的修复记录。每个修复均对应特定文件中的关键代码变更。

### 🔧 致命漏洞修复

| # | 致命漏洞 | 文件 | 修复内容 |
|---|----------|------|----------|
| 1 | PID 双重积分爆炸 | `src/hydrogel_vbd/control/field_controller.py` | 将增量式假 PID 重构为标准位置式（`E_z = clip(P+I+D, 0, E_max)`），新增双重抗积分饱和（裁剪限幅 + 饱和反算回退），根除积分项指数发散 |
| 2 | GIL 死亡锁 | `cpp/src/vbd_solver.cpp` + `src/hydrogel_vbd/gui/simulation_worker.py` | C++ 求解器降级为单步函数（无 while 循环），Python Worker 接管时间流逝与渲染降频逻辑，每步之间释放 GIL 以保持 GUI 响应 |
| 3 | 伪深拷贝 Segfault | `src/hydrogel_vbd/gui/simulation_worker.py` `_deep_copy_mesh()` | 对所有通过 `Eigen::Map` 映射的 Numpy 数组补全 `.copy()`（含 `dm_inv`、`tet_volumes`、`colors`、`node_mass`、`czm_state` 等），确保工作线程与主线程内存完全隔离 |

### ⚠️ 架构隐患修复

| # | 隐患 | 文件 | 修复内容 |
|---|------|------|----------|
| 4 | Jacobi 迭代退化 | `cpp/src/vbd_solver.cpp` | 在着色迭代循环前添加 TODO 注释，标记需将弹力与 Hessian 计算下沉入着色最内层以恢复真正 Gauss-Seidel 性能 |

### 🔧 额外修复

| # | 问题 | 文件 | 修复内容 |
|---|------|------|----------|
| 5 | C++ 悬空指针 | `src/hydrogel_vbd/core/state.py` `masses` 属性 | 移除 `.copy()` 返回临时对象，改为直接返回持久化 `self.node_mass` 引用；`node_mass` 为 None 时自动初始化 |
| 6 | Eigen 除以零炸弹 | `cpp/src/elastic_energy.cpp` `neo_hookean_pk1_stress()` | 在 `J≤1e-12` 惩罚分支中彻底移除 `F.inverse().transpose()` 调用，改用 `-2.0·inverted_penalty·(1-J)·F`（原始 F 矩阵），杜绝 Inf/NaN 毒化 |
| 7 | LayerResult API 过时 | `src/hydrogel_vbd/gui/simulation_worker.py` | 将旧版构造函数升级为匹配 `state.py` 最新 `@dataclass` 槽位的参数格式 |
| 8 | PID 文档字符串过时 | `src/hydrogel_vbd/control/field_controller.py` | `PIDFieldController` 文档从描述旧增量形式校正为准确的位置式 PID + 双重抗积分饱和策略 |

---

## 维护说明

- `.gitignore` 配置忽略 `outputs/`、`__pycache__/`、`.pytest_cache/`、编译产物等
- CAD 测试模型位于 `assets/test_models/`（`.STL` 和 `.STEP` 格式，测试通过相对路径引用）
- 配置文件位于 `configs/`，主配置为 `config.yaml`，子模块 JSON 可独立覆盖
- C++ 代码通过 `cpp/` 目录组织，pybind11 绑定在 `bindings/` 下单独维护
- GUI 启动入口：`python run_gui.py` 或 `python -m hydrogel_vbd.gui`
- 参数面板（16 项）定义于 `_PARAM_META` 列表，修改参数无需改动 `ParameterPanel._build_ui`
